{
  "name": "common",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "modules/common/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/modules/common",
        "main": "modules/common/src/index.ts",
        "tsConfig": "modules/common/tsconfig.lib.json",
        "assets": ["modules/common/*.md"]
      }
    },
    "publish": {
      "command": "node tools/scripts/publish.mjs common {args.ver} {args.tag} {args.otp}",
      "dependsOn": ["build"]
    },
    "install": {
      "command": "cd modules/common && npm install"
    }
  },
  "tags": []
}
